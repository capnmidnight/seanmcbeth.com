@page "{name}"
@model AppModel
@{
    ViewData["Title"] = Model.Name;
}

@section Head {
    @if (Model.IncludeThreeJS)
    {
        <bundle-js name="three" versioned />
    }
    <bundle-js name="@(Model.Name + "-app")" versioned />
}

@Html.AntiForgeryToken()